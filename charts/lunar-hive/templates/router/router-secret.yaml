apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcpx-hive.fullname" . }}-secret
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "mcpx-hive.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: router
    app.kubernetes.io/part-of: {{ include "mcpx-hive.name" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: Opaque
data:
  OIDC_ISSUER_URL: {{ .Values.global.oidc.issuerUrl | b64enc | quote }}
  OIDC_CLIENT_ID: {{ .Values.global.oidc.clientId | b64enc | quote }}
  OIDC_CLIENT_SECRET: {{ .Values.global.oidc.clientSecret | b64enc | quote }}
  SESSION_SECRET: {{ .Values.global.oidc.sessionSecret | b64enc | quote }}
  REDIS_URL: {{ .Values.global.redis.url | b64enc | quote }}
  REDIS_IS_CLUSTER: {{ .Values.global.redis.isCluster | toString | b64enc | quote }}

